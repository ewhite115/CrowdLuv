{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Talent creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('talent') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
