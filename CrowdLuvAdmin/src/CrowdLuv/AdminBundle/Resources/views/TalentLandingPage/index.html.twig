{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TalentLandingPage list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Message</th>
                <th>Image</th>
                <th>Messagetimestamp</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('talentlandingpage_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.message }}</td>
                <td>{{ entity.image }}</td>
                <td>{% if entity.messageTimestamp %}{{ entity.messageTimestamp|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('talentlandingpage_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('talentlandingpage_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('talentlandingpage_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
