{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Follower creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('follower') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
